var fs = require('fs');

/*
fs.createReadStream("../files/xayah.jpg")
    .pipe(fs.createWriteStream('xayah2stream.jpg'))
    .on('finish', function() {
        console.log('Arquivo escrito com stream');
    });
*/
